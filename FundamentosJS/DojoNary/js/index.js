function agregarLike(elemento)  {
    var likes = parseInt(elemento.innerText);
    likes++;
    elemento.innerText = likes;
}
function agregarHtml(elemento) {
    let contenedor = document.querySelector('".flex-2');
}